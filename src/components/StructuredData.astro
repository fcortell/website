---
export interface Props {
  type: "BlogPosting" | "Person" | "WebSite";
  data: any;
}

const { type, data } = Astro.props;

let structuredData = {};

if (type === "BlogPosting") {
  structuredData = {
    "@context": "https://schema.org",
    "@type": "BlogPosting",
    headline: data.title,
    description: data.description,
    author: {
      "@type": "Person",
      name: data.author || "Fermin Corteller",
      url: "https://fcortell.com/about",
    },
    datePublished: data.pubDatetime.toISOString(),
    dateModified: data.modDatetime?.toISOString() || data.pubDatetime.toISOString(),
    publisher: {
      "@type": "Person",
      name: "Fermin Corteller",
      logo: {
        "@type": "ImageObject",
        url: "https://fcortell.com/fermin-avatar.jpg",
      },
    },
    mainEntityOfPage: {
      "@type": "WebPage",
      "@id": data.url,
    },
    image: data.ogImage || "https://fcortell.com/fermin-avatar.jpg",
    articleSection: data.tags?.[0] || "Technology",
    keywords: data.tags?.join(", ") || "",
    wordCount: data.wordCount,
    timeRequired: data.readingTime,
  };
} else if (type === "Person") {
  structuredData = {
    "@context": "https://schema.org",
    "@type": "Person",
    name: "Fermin Corteller",
    url: "https://fcortell.com",
    image: "https://fcortell.com/fermin-avatar.jpg",
    sameAs: [
      "https://github.com/fcortell",
      "https://twitter.com/fcortell",
      "https://bsky.app/profile/fcortell.com",
    ],
    jobTitle: "Software Engineer",
    description:
      "Full stack software developer turned outdoorsman.",
  };
} else if (type === "WebSite") {
  structuredData = {
    "@context": "https://schema.org",
    "@type": "WebSite",
    name: "Fermin Corteller",
    url: "https://fcortell.com",
    description:
      "Full stack software developer turned outdoorsman.",
    author: {
      "@type": "Person",
      name: "Fermin Corteller",
    },
    potentialAction: {
      "@type": "SearchAction",
      target: "https://fcortell.com/search?q={search_term_string}",
      "query-input": "required name=search_term_string",
    },
  };
}
---

<script type="application/ld+json" set:html={JSON.stringify(structuredData)} />